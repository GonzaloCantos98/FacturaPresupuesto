// Configuración de Firebase (Consola de Firebase → Configuración del proyecto → Tus apps → Web).
// Estos valores no son secretos: la seguridad la dan el inicio de sesión y las reglas de Firestore.
// Mientras apiKey empiece por "PEGAR", la app funciona en modo local (datos solo en este navegador).
export const firebaseConfig = {
  apiKey: 'PEGAR_AQUI',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
